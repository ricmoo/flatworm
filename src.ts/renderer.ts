"use strict";
/*
import { Document } from "./document";

export type File = {
    filename: string;
    content: Buffer;
}

export abstract class Renderer {
    render(): Promise<Array<File>>;

    static checkHash(content: string, filename: string): void {
    
    }
}

export class HtmlRenderer {
    render(Document: Document): Array<File> {
        return null;
    }
}
*/
